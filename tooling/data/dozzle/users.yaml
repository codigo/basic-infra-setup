users:
  # "admin" here is username
  maumercado:
    name: "Mau"
    # bcrypt hash generated with: docker run --rm amir20/dozzle generate <user> --password <pass> --email <email>
    password: "{{ DOZZLE_PASSWORD }}"
    email: mau@codigo.sh
